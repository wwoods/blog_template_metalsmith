doctype html
html(lang="en")
  head
    title #{sitename}
  body
    if date !== undefined
      div Posted #{date}.
    each tag in tags
      if tag.name !== '_all'
        div Tagged #{tagsRootPath[tag.name]}.
    block template
    block teaser
    block contents

