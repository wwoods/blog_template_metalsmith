extends _shared.pug

block template-body
  .post.w3-mobile
    if attachedTo && attachedTo.length !== 0
      .attachedTo
        each parent, idx in attachedTo || []
          a(href=parent.path) #{parent.title}

    .title #{title}
    if date !== undefined
      div Posted #{date.getUTCFullYear()}-#{date.getUTCMonth()+1}-#{date.getUTCDate()}.
    block teaser
    if attachments && attachments.length
      .attachments
        each att in attachments
          a(href=att.path)
            span #{att.title}
    block content

    if tags.length !== 0
      div.tagList
        each tag, idx in tags
          a(href=tagGet(tag).path) #{tagGet(tag).rootPath}

